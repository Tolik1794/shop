@startuml

title Shop database - Payments

!include ./_common.puml

enum PaymentDirectionEnum {
    incoming
    outgoing
}

enum PaymentTypeEnum {
    cash
    card
    bank_transfer
    online
    other
}

enum PaymentStatusEnum {
    unpaid
    partially_paid
    paid
    overpaid
    refunded
}

entity Payment {
    * id: int
    * version: int
    --
    * direction: PaymentDirectionEnum
    * type: PaymentTypeEnum
    * amount: decimal(14, 4)
    * amountBase: decimal(14, 4)
    * paidAt: DateTimeImmutable
    comment: text?
    externalReference: string(255)?
    * currency: Currency
    * exchangeRateToBase: decimal(18, 8)
    * store: Store
    order: Order?
    purchase: Purchase?
    reversesPayment: Payment?
    createdBy: User?
    updatedBy: User?
    * createdAt: DateTimeImmutable
    * updatedAt: DateTimeImmutable
}

entity PaymentHistory {
    * id: int
    --
    * eventKey: string(100)
    * documentType: string(32)
    * documentId: int
    * title: string(255)
    description: text?
    * amount: decimal(14, 4)
    * amountBase: decimal(14, 4)
    * currencyCode: string(3)
    * direction: PaymentDirectionEnum
    * type: PaymentTypeEnum
    externalReference: string(255)?
    payload: json?
    * occurredAt: DateTimeImmutable
    actorNameSnapshot: string(255)?
    * store: Store
    order: Order?
    purchase: Purchase?
    * payment: Payment
    actor: User?
}



Store ||--o{ Payment : has payments
Currency ||--o{ Payment : payment currency
Order |o--o{ Payment : paid by
Purchase |o--o{ Payment : paid by
Payment |o--o| Payment : reversal of
User |o--o{ Payment : records/updates payments
PaymentDirectionEnum ||--o{ Payment : defines direction
PaymentTypeEnum ||--o{ Payment : classifies
Store ||--o{ PaymentHistory : has payment events
Order |o--o{ PaymentHistory : shows in timeline
Purchase |o--o{ PaymentHistory : shows in timeline
Payment ||--o{ PaymentHistory : creates event
User |o--o{ PaymentHistory : performs event
PaymentDirectionEnum ||--o{ PaymentHistory : event direction
PaymentTypeEnum ||--o{ PaymentHistory : event type

note right of Payment
  Universal payment table for customer payments and supplier payments.

  Direction:
  - incoming: money received, usually for Order.
  - outgoing: money paid, usually for Purchase.

  Rules:
  - exactly one of order or purchase should be filled.
  - Order payments are usually incoming.
  - Purchase payments are usually outgoing.
  - refunds and reversals use the opposite direction while type remains the payment method.
  - reversal corrections are linked to the original payment and keep its snapshot.
  - every reversal stores an explicit business reason in comment.
  - amount is in payment currency.
  - amountBase is stored in Store.baseCurrency using exchangeRateToBase snapshot.

  Do not rely only on Order.paidAt for payment state.
  Payment records are the source of truth for paid/remaining amounts.
end note

note right of Payment
  Payment records are the source of truth for financial payments.

  Order.paidAmountBase / Purchase.paidAmountBase and paymentStatus are denormalized cache fields
  for fast lists, filters, and dashboards.

  They should be recalculated from Payment rows after creating payments or explicit linked reversals.
  paidAt can store the date when the document became fully paid.
end note

note right of PaymentHistory
  Append-only financial activity timeline for payments attached to Order or Purchase.

  It records payment.recorded and payment.reversed events for new operations.
  Existing Payment rows are not automatically backfilled.

  documentType/documentId are denormalized timeline keys.
  order or purchase keeps the direct FK for current supported document types.
  Exactly one of order or purchase should be filled.

  title/description are display snapshots; payload keeps machine-readable context
  such as original_payment_id and reversal reason.
  Payment rows remain the financial source of truth; PaymentHistory is the audit/feed layer.
end note

@enduml
