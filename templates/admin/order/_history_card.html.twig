{% set reload_target = reload_target|default(false) %}
<div
        class="card order-history-card"
        {% if reload_target %}{{ stimulus_target('reload-card', 'historyCard') }}{% endif %}
        data-card-id="{{ entity.id }}"
>
    <div class="card-body">
        {{ include('admin/order/_history.html.twig') }}
    </div>
</div>
