{% extends 'admin/_store_components/layout.html.twig' %}

{% block title %}Customer{% endblock %}

{% block navbar_breadcrumb %}
    {{ include('admin/_components/_resource_breadcrumb.html.twig', {
        section_label: 'Customers',
        section_url: path('app_admin_customer_index', {store_id: app.request.attributes.get('store_id')}),
        current_label: entity.id ? entity.fullName : 'New'
    }) }}
{% endblock %}

{% block main_content %}
    <main class="content" {{ stimulus_controller('select-two') }}>
        <div class="container-fluid p-0">
            {{ form_start(form, {attr: {'data-select-two-target': 'form'}}) }}
            <div class="row">
                <div class="card">
                    <div class="card-header">
                        <h5 class="card-title mb-0">{{ entity.id ? entity.fullName : 'New' }}</h5>
                    </div>
                    <div class="card-body">
                        {% if form.vars.errors|length > 0 %}
                            <div class="form-error">
                                {{ form_errors(form) }}
                            </div>
                        {% endif %}

                        {% for name, widget in form %}
                            {{ form_row(widget) }}
                        {% endfor %}

                    </div>
                </div>
                {{ include('admin/_components/_form_actions.html.twig', {
                    cancel_url: path('app_admin_customer_index', {store_id: app.request.attributes.get('store_id')})
                }) }}
            </div>
            {{ form_end(form, {'render_rest': false}) }}
        </div>
    </main>
{% endblock %}
