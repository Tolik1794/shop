<div class="card" {{ stimulus_target('reload-card', 'historyCard') }} data-card-id="{{ entity.id }}">
	<div class="card-header">
		<h5 class="card-title mb-0">{{ entity.number }} · History</h5>
	</div>
	<div class="card-body">
		<div class="order-discussion-scroll-frame">
			{{ include('admin/_components/document_history/_timeline.html.twig') }}
		</div>
	</div>
</div>
